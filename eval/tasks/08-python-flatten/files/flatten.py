def flatten(xs):
    """Flatten an arbitrarily nested list into a single flat list.

    Non-list elements are kept as-is in the order they appear.
    Strings are treated as atomic values, not iterated.
    """
    raise NotImplementedError
